# Angular frontend environment hints
# Angular currently uses src/environments/*.ts for build-time settings.
# Keep this file for Docker/local conventions and future runtime injection.

FRONTEND_PORT=4200
API_BASE_URL=http://localhost:3000/api
APP_NAME=eAudit QMS
