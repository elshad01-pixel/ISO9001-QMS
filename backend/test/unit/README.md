# Backend unit test structure

Unit tests are colocated with source modules under `backend/src/**/*.spec.ts`.

Examples added:
- document workflow validation
- CAPA lifecycle validation
- audit findings validation
- RBAC permission guard behavior
- audit log service behavior
